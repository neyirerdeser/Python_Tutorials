import random
import sys
import os

# lists but cannot be changed afterwards

pi_tuple = (3,1,4,1,5,9)

# if you really need to change, convert into list change convert back to tuple
# but thats not really a thing

new_tuple = list(pi_tuple)
new_list = tuple(new_tuple)

len(new_list)
min()
max()
